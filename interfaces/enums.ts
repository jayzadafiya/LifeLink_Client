export enum Gender {
  Male = "male",
  Female = "female",
  Other = "other",
}

export enum ApprovalStatus {
  Pending = "pending",
  Approved = "approved",
  Cancelled = "cancelled",
}
export enum BookingStatus {
  Pending = "pending",
  Complate = "complate",
  Cancelled = "cancelled",
}
